openapi: 3.1.0
info:
  title: raphael-connectors
  version: 0.1.0
  description: Native connectors, sync jobs, credentials
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: OK
